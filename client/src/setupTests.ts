// src/setupTests.js

// 1. Import jest-dom to add custom matchers for assertions on DOM elements
import '@testing-library/jest-dom';

// 2. Mock global fetch if needed
// global.fetch = jest.fn(() =>
//   Promise.resolve({
//     json: () => Promise.resolve({}),
//   })
// );

// 3. You can add any global setup or mocks here (e.g., mock APIs, timers)
